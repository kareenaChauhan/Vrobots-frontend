export interface TeamMember {
  name: string;
  role: string;
  image: string;
}

export interface AboutPageProps {
  className?: string;
}

export interface StatisticProps {
  value: string;
  label: string;
}